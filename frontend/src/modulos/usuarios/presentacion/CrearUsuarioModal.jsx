import React from 'react';

export function CrearUsuarioModal({ isOpen, onClose }) {
  if (!isOpen) return null;
  return (
    <div>
      {/* Modal para que Juliana implemente HU-0101 */}
    </div>
  );
}
